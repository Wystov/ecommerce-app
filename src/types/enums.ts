enum NamePages {
  Home = 'Home',
  AboutUs = 'About us',
  Catalog = 'Catalog',
  Login = 'Login',
  Logout = 'Logout',
  Registration = 'Registration',
}

const enum PathPages {
  Home = '/',
  AboutUs = '/AboutUs',
  Catalog = '/Catalog',
  Login = '/Login',
  Logout = '/',
  Registration = '/Registration',
}

export { NamePages, PathPages };
